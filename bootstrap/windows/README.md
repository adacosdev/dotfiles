# Windows bootstrap layer

This directory defines the future home for Windows-specific provisioning.

Treat Windows as a first-class platform with explicit design choices instead of
forcing Linux assumptions onto it.

Potential responsibilities:

- `winget` / `scoop` / `choco` setup
- PowerShell profile bootstrapping
- Windows Terminal integration
- WSL companion setup where applicable
