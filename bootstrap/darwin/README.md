# macOS bootstrap layer

This directory defines the future home for macOS-specific provisioning.

Nothing is automated here yet. When macOS support is added, keep the same
responsibility boundaries used on Linux:

- Homebrew packages and casks
- shell bootstrap
- fonts and terminal setup
- editor tooling
